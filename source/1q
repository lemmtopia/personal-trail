#include <stdio.h>
#include "lemonic.h"

int main(void)
{
    v2 v ={1.2, 0.9};
    printf("Vec = {%f, %f}", v.x, v.y);
    v = v2_mult_scalar(v, 3);
    printf("Vec = {%f, %f}", v.x, v.y);
    v = v2_normalize(v, 3);
    printf("Vec = {%f, %f}", v.x, v.y);
}
